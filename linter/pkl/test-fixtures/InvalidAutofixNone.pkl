amends "../schema/BpmnRule.pkl"

import "../schema/RuleCategory.pkl"

name = "Invalid Autofix"
category = RuleCategory.Activity
sequence = 99

intent = "This fixture should fail because an auto-fixable rule cannot use the none strategy."
forModellers = "This fixture should fail because an auto-fixable rule cannot use the none strategy."
forAI = "This fixture should fail because an auto-fixable rule cannot use the none strategy."

targetElements = List("bpmn:Task")
severity = "warning"
errorMessages = new {
  ["default"] = "This fixture should fail"
}

autoFixable = true
fixStrategy = "none"
