amends "pkl:test"

import "../schema/generated/NodeProperty.pkl"
import "../schema/generated/NodeTypeName.pkl"

local nodeType: NodeTypeName.NodeTypeName = "bpmn:StartEvent"
local nodeProperty: NodeProperty.NodeProperty = "name"

facts {
  ["generated node type enum uses BPMN QName naming"] {
    nodeType == "bpmn:StartEvent"
  }

  ["generated node property enum contains annotated properties"] {
    nodeProperty == "name"
  }
}
