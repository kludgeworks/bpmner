amends "../schema/BpmnRule.pkl"

import "../schema/RuleCategory.pkl"

name = "Missing Intent"
category = RuleCategory.Name
sequence = 99

intent = ""
forModellers = "This fixture should fail because intent is empty."
forAI = "This fixture should fail because intent is empty."

targetElements = List("bpmn:Task")
severity = "warning"
errorMessages = new {
  ["default"] = "This fixture should fail"
}
