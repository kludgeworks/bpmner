spring:
  application:
    name: bpmner
  main:
    web-application-type: none
  profiles:
    group:
      anth:
        - anthropic
        - verbose
      gh:
        - github
        - verbose

embabel:
  agent:
    shell:
      web-application-type: none
      interactive:
        enabled: true
        history-enabled: true

bpmner:
  max-attempts: 5
  logging:
    dir: ${BPMNER_LOG_DIR:${LOG_DIR:logs}}
    dump-artifacts: false
  lint:
    extends:
      - plugin:klm/recommended-error
    rules:
      klm/act-01-verb-object-name: error
      klm/act-02-activity-label-capitalization: error
      klm/act-03-discouraged-business-verbs: error
      klm/act-12-loop-task-annotation: error
      klm/act-13-mi-task-annotation: error
