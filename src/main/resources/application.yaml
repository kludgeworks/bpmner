spring:
  application:
    name: bpmner
  main:
    web-application-type: none
  profiles:
    group:
      anth:
        - anthropic
        - verbose
      gh:
        - github
        - verbose
      web:
        - web

embabel:
  models:
    llms:
      generator: gpt-4.1
      repairer: gpt-4.1-mini
  agent:
    shell:
      web-application-type: none
      interactive:
        enabled: true
        history-enabled: true

bpmner:
  max-attempts: 5
  logging:
    dir: ${BPMNER_LOG_DIR:${LOG_DIR:${BUILD_WORKING_DIRECTORY:${user.dir}}/logs}}
    dump-artifacts: false
  repair:
    abbreviations:
      ITBL: ineligible-to-board list
      PNR: booking reference
      FFP: frequent flyer programme
  lint:
    extends:
      - plugin:klm/recommended-error
    rules:
      "[klm/act-verb-object-name]": error
      "[klm/act-activity-label-capitalization]": error
      "[klm/act-discouraged-business-verbs]": error
      "[klm/act-loop-task-annotation]": error
      "[klm/act-mi-task-annotation]": error
